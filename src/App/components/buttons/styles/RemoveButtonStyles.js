// react
import { StyleSheet } from 'react-native';
// libraries

// own component

// styles

export default StyleSheet.create({
    buttonContainer: {
        width: 48,
        height: 48,
        justifyContent: 'center',
        marginRight: 5,
        alignItems: 'flex-end',
    }
})